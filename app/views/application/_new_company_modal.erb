    tag.button(
      onclick: "handleAddCompany(event, '#{ p[:add_to_select_class] }', '#{ p[:dialog_id] }')",
      class: "btn btn-info management-button",
      title: "Додати нову команію в довідник",
      'data-toggle': "modal",
      'data-target': "##{p[:dialog_id]}"
    ){ tag.span( nil, class: "fa fa-plus")}+ 
    tag.div(
      id: p[:dialog_id],
      class: "modal fade",
      title: "Додати компанію",
      tabindex: "-1",
      role: "dialog",
      'aria-labelledby': "exampleModalLabel",
      'aria-hidden':"true"
    ){tag.div( class: "modal-dialog",role: "document"){
        tag.div( class: "modal-content"){
          tag.div( class: "modal-body"){
            tag.input('input here', class: "my-input")+
            tag.input('input2 here', class: "my-input")
          }+
          tag.div(class: "modal-footer"){
            tag.button('Save changes', type: "button", class: "btn btn-primary")+
            tag.button('Close', type: "button", class: "btn btn-secondary", 'data-dismiss': "modal" )
          }
        }
      }
    }
