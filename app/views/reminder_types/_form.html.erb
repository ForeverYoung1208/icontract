<%= form_with(model: reminder_type, local: true) do |form| %>
  <% if reminder_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reminder_type.errors.count, "error") %> prohibited this reminder_type from being saved:</h2>

      <ul>
      <% reminder_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :reminder_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :reminder_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
