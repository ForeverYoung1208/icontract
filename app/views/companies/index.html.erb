<p id="notice"><%= notice %></p>

<h1>Довідник компаній</h1>

<table  class="table">
  <thead class="thead-default">
    <tr>
      <th>Id</th>
      <th>Найменування</th>
      <th>Адреса</th>
      <th>Код ЄДРПОУ</th>
      <th>Керування</th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.id %></td>
        <td><%= company.name %></td>
        <td><%= company.address %></td>
        <td><%= company.edrpou %></td>
        <td> 
          <%= management_links( company ) %>

        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Створити нову', new_company_path %>
